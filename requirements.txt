# Runtime — installed in the Docker image (see Dockerfile)

fastapi==0.138.0
pydantic==2.13.4
pydantic-settings==2.14.2
uvicorn[standard]==0.49.0

# Used in Matching engine
sortedcontainers==2.4.0

# Database (async app runtime)
sqlalchemy==2.0.51
asyncpg==0.31.0

# AWS / LocalStack
boto3==1.42.59